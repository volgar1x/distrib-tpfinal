#ifndef CONFIG_H
#define CONFIG_H

/** The port the server will listen to. */
#define PORT    64646
/** The amount of incoming parallel connections. */
#define BACKLOG 10
/** The size of a client's buffer. */
#define BUF_LEN 64

#endif // CONFIG_H
